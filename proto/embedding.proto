syntax = "proto3";

package userProfile;

import "google/protobuf/empty.proto";

message ProfileResponse {
  string status = 1;
}

service UserProfile {
  rpc CreateUserProfile(google.protobuf.Empty) returns (ProfileResponse) {}
}